{
  "name": "svg-nav",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript SVG pan/zoom with hover controls and keyboard/mouse interactions",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json"
  }
}
